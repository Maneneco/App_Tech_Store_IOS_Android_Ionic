import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ipvc.Intech',
  appName: 'Intech',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
